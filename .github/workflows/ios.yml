name: iOS Build (Tauri) Debug + Cache

on:
  push:
    branches:
      - ios

jobs:
  init-and-build-ios:
    runs-on: macos-latest

    steps:
      # ソースコード取得
      - name: Checkout repository
        uses: actions/checkout@v3

      # Node.js / npm 環境
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'

      # Xcode 環境確認（ライセンス承認など）
      - name: Setup Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app
          sudo xcodebuild -license accept

      # Cargo registry と target をキャッシュ
      - name: Cache Cargo
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ hashFiles('**/Cargo.lock') }}

      # Tauri CLI インストール (npm 版で高速)
      - name: Install Tauri CLI
        run: npm install -g @tauri-apps/cli

      # Tauri CLI が PATH にあるか確認
      - name: Check Tauri CLI
        run: which tauri && tauri -V

      # 依存モジュールインストール
      - name: Install dependencies
        run: npm ci

      # iOS プロジェクト初期化
      - name: Initialize iOS project
        run: cargo tauri ios init -v

      # iOS ビルド (Release + 詳細ログ)
      - name: Build iOS IPA
        run: cargo tauri ios build -- --release -v

      # IPA を Artifact として保存
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Tauri-iOS-IPA
          path: src-tauri/gen/apple/target/ios/release/*.ipa